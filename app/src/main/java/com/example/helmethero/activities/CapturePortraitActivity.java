package com.example.helmethero.activities;

import com.journeyapps.barcodescanner.CaptureActivity;

/**
 * Custom ZXing CaptureActivity to force portrait orientation.
 * No extra code needed!
 */
public class CapturePortraitActivity extends CaptureActivity {
    // Manifest setting akan lock orientation pada portrait
}
